# Python dependencies for ShellSim_3D on Euler.
# Pinned to be compatible with the stack/2024-06 + python_cuda/3.11.6 module
# (Python 3.11, CUDA 12.1).
#
# Install with:
#   module load stack/2024-06 python_cuda/3.11.6 eth_proxy
#   python -m venv $HOME/venvs/shellsim
#   source $HOME/venvs/shellsim/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements_euler.txt

# --- Core scientific stack ---
numpy>=1.26,<2.0
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
scikit-learn>=1.3
h5py>=3.10
pyDOE>=0.3.8

# --- GPU array library used by the sampler ---
# cupy-cuda12x matches the CUDA 12.x runtime exposed by python_cuda/3.11.6.
cupy-cuda12x>=13.0

# --- Deep learning stack used by the trainer ---
# Use the official CUDA 12.1 PyTorch wheels.
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.2,<2.6
lightning>=2.2

# --- Experiment tracking ---
wandb>=0.16

# --- Plotting / interactive figures used by some helpers ---
plotly>=5.18
